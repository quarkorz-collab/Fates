# KaTeX distribution

This directory contains the browser distribution of KaTeX 0.18.1 from the
`katex` npm package. Fates uses the minified JavaScript, CSS, and bundled fonts
for offline local math rendering. KaTeX is licensed under the MIT License; see
`LICENSE` in this directory.
